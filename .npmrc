# Enable hoisting of dependencies
hoist=true

# Use a shared workspace lockfile
shared-workspace-lockfile=true

# Ensure workspace packages are symlinked
link-workspace-packages=true

# Use workspace protocol for local dependencies
save-workspace-protocol=true

# Prevent creation of individual node_modules in packages
shamefully-hoist=true
